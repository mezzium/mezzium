---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mezzium
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mezzium
  template:
    metadata:
      labels:
        app: mezzium
    spec:
      containers:
        - name: mezzium
          image: shuliakovsky/mezzium:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: ADMIN_API_KEY
              value: changeme
            - name: SERVER_PORT
              value: "8080"
            - name: BOOTSTRAP_URL
              value: https://mezzium.example.com
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
---
apiVersion: v1
kind: Service
metadata:
  name: mezzium
spec:
  selector:
    app: mezzium
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mezzium
spec:
  rules:
    - host: mezzium.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mezzium
                port:
                  number: 80
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: mezzium
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: mezzium
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
