version: '3.8'

services:
  mezzium:
    image: shuliakovsky/mezzium:${VERSION:-0.0.1}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
    container_name: mezzium
